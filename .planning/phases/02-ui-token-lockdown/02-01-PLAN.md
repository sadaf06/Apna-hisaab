---
phase: 02-ui-token-lockdown
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - app/src/main/java/com/example/ui/theme/Palette.kt
  - app/src/main/java/com/example/data/CategoryConfig.kt
  - app/src/main/java/com/example/ui/components/GlassUi.kt
  - app/src/main/java/com/example/ui/screens/KahaniScreen.kt
  - app/src/main/java/com/example/ui/theme/Theme.kt
  - .github/workflows/build.yml
autonomous: true
requirements: [color-token-lockdown]
must_haves:
  truths:
    - "Pillar 3 Color re-audit scores 4/4 on UI-REVIEW re-run (was 2/4)"
    - "All 15 category chips render distinct colors via Palette.Category.* with no visual regression"
    - "CI fails if any Color(0x) appears outside Palette.kt"
  artifacts:
    - path: "app/src/main/java/com/example/ui/theme/Palette.kt"
      provides: "Single source of truth — Category/Glass/Brand objects contain all raw hexes"
      contains: "object Category"
    - path: "app/src/main/java/com/example/data/CategoryConfig.kt"
      provides: "CategoryConfig references Palette.Category.* only, zero raw Color(0x"
      contains: "Palette.Category"
    - path: ".github/workflows/build.yml"
      provides: "Palette token gate step before Build Release APK"
      contains: "Enforce Palette token gate"
  key_links:
    - from: "app/src/main/java/com/example/data/CategoryConfig.kt"
      to: "app/src/main/java/com/example/ui/theme/Palette.kt"
      via: "CategoryInfo color = Palette.Category.*"
      pattern: "Palette\\.Category\\."
    - from: ".github/workflows/build.yml"
      to: "app/src"
      via: "grep -R Color\\(0x.*grep -v Palette\\.kt fails build"
      pattern: "Enforce Palette token gate"
---

<objective>
Eliminate raw Color(0xFF...) outside Palette.kt — 15 CategoryConfig hexes + Glass translucent whites + WhatsApp brand greens + mood/Surface neutrals unified through Palette single source per D-01 Color Single Source of Truth.
Purpose: Achieve Pillar 3 Color 4/4 on re-audit; any future palette shift is one-file change.
Output: Palette.Category/Glass/Brand objects + migrated consumers + blocking CI gate.
</objective>

<execution_context>
@C:/Users/sisid/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/sisid/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-ui-token-lockdown/02-CONTEXT.md
@.planning/phases/02-ui-token-lockdown/02-REVIEWS.md
@K:/apna-hisaab/UI-REVIEW.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Refactor Palette.Category to semantic unification (not hex relocation)</name>
  <read_first>
    - K:/apna-hisaab/app/src/main/java/com/example/ui/theme/Palette.kt
    - K:/apna-hisaab/app/src/main/java/com/example/data/CategoryConfig.kt
    - K:/apna-hisaab/UI-REVIEW.md
  </read_first>
  <action>
    Refactor Palette.kt object Category per D-01 — UNIFY to semantic Palette tokens, do NOT merely relocate raw hexes.
    - Khana (🍽️) -> Palette.PurpleDeep (0xFF8B5CF6 exact, keep hex here as canonical PurpleDeep alias)
    - GharKharch (🏠) -> Palette.Teal (0xFF2DD4BF / 0xFF14B8A6 unify to Teal)
    - Rent (🏠) -> Palette.Purple (0xFF4F46E5 indigo drift unified to Palette.Purple 0xFFA78BFA)
    - EMI (💳) -> Palette.Purple (0xFF6366F1 -> Palette.Purple)
    - Petrol (⛽) -> Palette.Warning (0xFFF59E0B -> Warning 0xFFFBBF24)
    - Safar (🚗) -> Palette.Success or Palette.Teal (0xFF3B82F6 blue -> Palette.Teal)
    - Masti (🎮) -> Palette.Danger (0xFFEC4899 pink -> Danger 0xFFF87171 or keep distinct but reference Palette.Danger family)
    - Shopping (🛍️) -> Palette.Success (0xFF22C55E -> Success 0xFF34D399)
    - Health (💊) -> Palette.Danger (0xFFDC2626 -> Danger)
    - Padhai (📚) -> Palette.Purple (0xFF60A5FA -> Palette.Purple)
    - Personal (💇) -> Palette.Teal (0xFF10B981 -> Teal)
    - Gift (🎁) -> Palette.Danger (0xFFF43F5E -> Danger)
    - Savings (💰) -> Palette.Success (0xFF22C55E -> Success)
    - Pooja (🙏) -> Palette.Warning (0xFFEAB308 -> Warning)
    - Recharge (📱) -> Palette.TextFaint or Palette.BorderSoft (0xFF0F172A navy drift ELIMINATED — map to TextFaint 0x59FFFFFF or SurfaceInset, document drift fix)
    - Other (📦) -> Palette.Warning (0xFFF97316 orange -> Warning family)
    - Keep raw Color(0x) ONLY inside Palette.kt; every Category val must be either `Palette.<semantic>` or a single canonical hex that IS the semantic definition. Add KDoc comment "Recharge navy eliminated: unified to TextFaint per UI-REVIEW drift".
    Ensure object Glass { White06 0.06, White10 0.10, White12 0.12, White20 0.20, Black28 0.28, plus White15/25/40 existing } uses Color.White/Black copy(alpha) — these hex-ish copies are ONLY in Palette.kt. Ensure object Brand { WhatsApp 0xFF25D366, WhatsAppDark 0xFF128C7E } exists. Do NOT redesign glassFill/accentBorderBrush visuals.
  </action>
  <verify>
    <automated>grep -R "Color(0x" --include="*.kt" app/src | grep -v "Palette.kt" | wc -l | grep -q "^0$" && echo "gate passed: no raw hex outside Palette" || (grep -R "Color(0x" --include="*.kt" app/src | grep -v "Palette.kt"; exit 1)</automated>
  </verify>
  <done>Palette.kt contains object Category with 16 vals mapped to semantic tokens (Recharge unified, not relocated), object Glass, object Brand; grep -R Color(0x outside Palette.kt == 0</done>
</task>

<task type="auto">
  <name>Task 2: Migrate CategoryConfig + GlassUi + Kahani mood + Theme surface to Palette tokens</name>
  <read_first>
    - K:/apna-hisaab/app/src/main/java/com/example/data/CategoryConfig.kt
    - K:/apna-hisaab/app/src/main/java/com/example/ui/components/GlassUi.kt
    - K:/apna-hisaab/app/src/main/java/com/example/ui/screens/KahaniScreen.kt
    - K:/apna-hisaab/app/src/main/java/com/example/ui/theme/Theme.kt
    - K:/apna-hisaab/app/src/main/java/com/example/ui/theme/Palette.kt
  </read_first>
  <action>
    Per D-01 and specifics mapping:
    - CategoryConfig.kt: ensure every CategoryInfo references Palette.Category.* (already does) — keep fallback getCategoryByName returning Palette.Category.Other; remove any leftover Color import if unused.
    - GlassUi.kt: replace Color.White.copy(alpha=0.12f/0.20f/0.06f) with Palette.Glass.White12/White20/White06, Color.Black.copy(0.28f) with Palette.Glass.Black28 in glassShadow and BlurLockable (lines 303-305, 295). Keep glassFill/accentBorderBrush definitions referencing Palette.SurfaceHigh/Low and BorderSoft.
    - KahaniScreen.kt: replace WhatsAppGreenDark/Green hex vals (0xFF128C7E/0xFF25D366 at ~70-71) with Palette.Brand.WhatsAppDark/Green in share bitmap generation; replace getMoodAttributes duplicate hexes (0xFF34D399, 0xFFFBBF24, 0xFFFF8B8B, 0xFF60A5FA, 0xFF9AA3B8) with Palette.mood(key) + Palette.Success/Warning/Danger/TextFaint map; remove Color(0xFF161B22) if present.
    - Theme.kt: verify surface = Palette.BaseTop (not Color(0xFF161B22)) and onSurfaceVariant = Palette.TextSecondary (already done); remove import androidx.compose.ui.graphics.Color if no longer needed.
  </action>
  <verify>
    <automated>grep -R "Color(0x\|Color\.White\|Color\.Black" --include="*.kt" app/src/main/java/com/example/data app/src/main/java/com/example/ui/components app/src/main/java/com/example/ui/screens/KahaniScreen.kt app/src/main/java/com/example/ui/theme/Theme.kt 2>/dev/null | grep -v "Palette.kt" | wc -l | xargs echo "remaining literals:"; test $(grep -R "Palette\.Category\.\|Palette\.Glass\.\|Palette\.Brand\." --include="*.kt" app/src | wc -l) -ge 15 && echo "Palette refs present"</automated>
  </verify>
  <done>CategoryConfig zero raw hex; GlassUi uses Palette.Glass.* exclusively; KahaniScreen WhatsApp share uses Palette.Brand.* and mood via Palette.mood(); Theme surface via Palette.BaseTop; no Color(0x/White/Black literals outside Palette.kt in migrated files</done>
</task>

<task type="auto">
  <name>Task 3: Enforce blocking CI Palette gate (sequenced — owns build.yml in wave 1)</name>
  <read_first>
    - K:/apna-hisaab/.github/workflows/build.yml
  </read_first>
  <action>
    Ensure build.yml contains single blocking gate step named "Enforce Palette token gate" immediately before Build Release APK. The step must FAIL (exit 1) if any raw Color(0x outside Palette.kt: run `if grep -R "Color(0x" --include="*.kt" app/src | grep -v "Palette.kt" | grep -q .; then echo "::error::Raw Color(0x) outside Palette.kt"; grep -R "Color(0x" --include="*.kt" app/src | grep -v "Palette.kt"; exit 1; fi; echo "Palette gate passed"`. Do NOT add typography/spacing gates here — those belong to 02-02 and 02-03 respectively to avoid parallel build.yml race. This plan owns the Palette gate insertion point; downstream plans will append their gates sequentially due to wave dependency. Verify step ordering: Decode Keystore -> Create local.properties -> Grant execute -> Enforce Palette token gate -> (future Typography/Spacing gates) -> Build Release APK.
  </action>
  <verify>
    <automated>grep -q "Enforce Palette token gate" .github/workflows/build.yml && grep -B2 -A3 "Enforce Palette" .github/workflows/build.yml | head -20; echo "---order check---"; grep -n "Enforce Palette\|Build Release APK" .github/workflows/build.yml</automated>
  </verify>
  <done>build.yml contains blocking Palette gate before assembleRelease; gate fails on raw hex outside Palette.kt and passes when clean; step ordering correct for sequential wave appends</done>
</task>

</tasks>

<verification>
- `grep -R "Color(0x" --include="*.kt" app/src` → only app/src/main/java/com/example/ui/theme/Palette.kt lines
- `./gradlew :app:assembleRelease` succeeds with no R8 color stripping (Palette tokens retained via keep)
- Re-run UI-REVIEW P3 checklist: CategoryConfig 15 hexes eliminated, Glass translucents via Palette.Glass, Brand greens via Palette.Brand, mood via Palette.mood — P3 scores 4/4
</verification>

<success_criteria>
Pillar 3 Color re-audit 4/4: zero raw hex outside Palette.kt, CategoryConfig uses semantic Palette.Category.* (Recharge drift unified), GlassUi uses Palette.Glass.*, KahaniScreen uses Palette.Brand.* and Palette.mood(), blocking CI gate enforces discipline and phase goal 20+/24 trajectory achieved via Color pillar fixed.
</success_criteria>

<output>
Create `.planning/phases/02-ui-token-lockdown/02-01-SUMMARY.md` when done
</output>
