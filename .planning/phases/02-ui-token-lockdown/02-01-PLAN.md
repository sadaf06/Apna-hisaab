---
wave: 1
depends_on: []
files_modified:
  - app/src/main/java/com/example/ui/theme/Palette.kt
  - app/src/main/java/com/example/data/CategoryConfig.kt
  - app/src/main/java/com/example/ui/components/GlassUi.kt
  - app/src/main/java/com/example/ui/screens/KahaniScreen.kt
  - app/src/main/java/com/example/ui/theme/Theme.kt
  - .github/workflows/build.yml
autonomous: true
requirements: [color-token-lockdown]
---

# Plan 02-01: Color — Eliminate Raw Hexes, Route Through Palette

**Objective:** No `Color(0xFF…)` outside `Palette.kt` — 15 CategoryConfig hexes + Glass whites + WhatsApp greens + duplicate mood colors unified.

**Gap:** UI-REVIEW P3 Color 2/4 — `grep "Color(0x" → 18 hits` breaks own contract `Palette.kt:13 "never raw Color(0x...)"`

## Tasks

### Task 1: Extend Palette with Category + Glass + Brand tokens
<read_first>
- K:/apna-hisaab/app/src/main/java/com/example/ui/theme/Palette.kt
- K:/apna-hisaab/app/src/main/java/com/example/data/CategoryConfig.kt
</read_first>
<action>
Add to Palette.kt:
- object Category { val Khana = PurpleDeep; val GharKharch = Teal; val Rent = Color(0xFF4F46E5) -> map to Palette.Purple etc OR keep hex but inside Palette only; define Khana, GharKharch, Rent, EMI, Petrol, Safar, Masti, Shopping, Health, Padhai, Personal, Gift, Savings, Pooja, Recharge, Other as vals referencing existing Palette semantic or new Category palette (migrate hexes here) }
- object Glass { val White06=Color.White.copy(0.06f), White10=0.10f, White12=0.12f, White20=0.20f, Black28=Color.Black.copy(0.28f) }
- object Brand { val WhatsApp=Color(0xFF25D366); val WhatsAppDark=Color(0xFF128C7E) }
Keep all raw hexes ONLY in Palette.kt.
</action>
<acceptance_criteria>
- Palette.kt contains `object Category` with 16 vals and `object Glass` and `object Brand`, no other file contains `Color(0xFF`
- `grep -R "Color(0x" --include="*.kt" app/src | grep -v "Palette.kt"` exits 0 (empty)
</acceptance_criteria>

### Task 2: Migrate CategoryConfig + GlassUi + Kahani mood duplicates
<read_first>
- K:/apna-hisaab/app/src/main/java/com/example/data/CategoryConfig.kt
- K:/apna-hisaab/app/src/main/java/com/example/ui/components/GlassUi.kt
- K:/apna-hisaab/app/src/main/java/com/example/ui/screens/KahaniScreen.kt
- K:/apna-hisaab/app/src/main/java/com/example/ui/theme/Theme.kt
</read_first>
<action>
- CategoryConfig.kt: replace `CategoryInfo("Khana","🍽️",Color(0xFF8B5CF6))` with `Palette.Category.Khana` (and so on for all 16). `getCategoryByName` fallback returns `Palette.Category.Other`.
- GlassUi.kt: replace `Color.White.copy(alpha=0.12f/0.20f/0.06f)` with `Palette.Glass.White12/White20/White06`, `Color.Black` with `Palette.Glass.Black28` in glassShadow, `BlurLockable`.
- KahaniScreen.kt: replace `WhatsAppGreenDark/Green` hex vals with `Palette.Brand.WhatsApp*`, replace `getMoodAttributes` duplicate hexes with `Palette.mood(key)` + `Palette.Success/Warning/Danger` map. Remove `Color(0xFF161B22)` in Theme.kt surface → `Palette.SurfaceInset` or `BaseTop`.
- Theme.kt: change `surface = Color(0xFF161B22)` to `Palette.BaseTop` or `Palette.SurfaceLow`.
</action>
<acceptance_criteria>
- CategoryConfig.kt has zero `Color(0x` literals
- GlassUi.kt has zero `Color.White` / `Color.Black` literals, uses `Palette.Glass.*`
- KahaniScreen.kt WhatsApp share uses `Palette.Brand.*`
- `git grep "Color(0x" app/src` shows only Palette.kt line numbers
</acceptance_criteria>

### Task 3: Add CI grep gate
<read_first>
- K:/apna-hisaab/.github/workflows/build.yml
</read_first>
<action>
Add step before Build Release APK:
- name: Enforce Palette token gate
  run: |
    if grep -R "Color(0x" --include="*.kt" app/src | grep -v "Palette.kt" | grep -q .; then echo "::error::Raw Color(0x) outside Palette.kt"; grep -R "Color(0x" --include="*.kt" app/src | grep -v "Palette.kt"; exit 1; fi
    echo "Palette gate passed"
</action>
<acceptance_criteria>
- .github/workflows/build.yml contains "Enforce Palette token gate" step before assembleRelease
- CI fails if raw hex added outside Palette.kt, passes when only Palette.kt has hexes
</acceptance_criteria>

## Verification
- `grep -R "Color(0x" --include="*.kt" app/src` → only Palette.kt
- `./gradlew :app:assembleRelease` succeeds, no R8 color stripping
- UI-REVIEW re-audit P3 Color → 4/4

## must_haves
- No raw hex outside Palette.kt
- CategoryConfig uses Palette.Category.*
- CI gate enforces token discipline
