---
phase: 02-ui-token-lockdown
plan: "02"
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - app/src/main/java/com/example/ui/theme/Type.kt
  - app/src/main/java/com/example/ui/screens/AajScreen.kt
  - app/src/main/java/com/example/ui/screens/KahaniScreen.kt
  - app/src/main/java/com/example/ui/screens/SapnaScreen.kt
  - app/src/main/java/com/example/ui/screens/SettingsScreen.kt
  - app/src/main/java/com/example/ui/screens/HisaabScreen.kt
  - .github/workflows/build.yml
autonomous: true
requirements: [typography-lockdown]
must_haves:
  truths:
    - "Pillar 4 Typography re-audit scores 4/4 on UI-REVIEW re-run (was 2/4)"
    - "Hero amount 44sp and micro badge 10sp render via typography tokens, not inline overrides — visual unchanged but tokenized"
    - "CI fails if any screen contains copy(fontSize= or fontSize = outside Type.kt"
  artifacts:
    - path: "app/src/main/java/com/example/ui/theme/Type.kt"
      provides: "Collapsed type scale ≤7 styles, 3 weights, displayHero 44sp + labelMicro 10sp tokens"
      contains: "displayHero"
    - path: ".github/workflows/build.yml"
      provides: "Blocking typography gate step appended after Palette gate"
      contains: "Enforce typography gate"
  key_links:
    - from: "app/src/main/java/com/example/ui/screens/AajScreen.kt"
      to: "app/src/main/java/com/example/ui/theme/Type.kt"
      via: "MaterialTheme.typography.displayHero/labelMicro not copy(fontSize="
      pattern: "MaterialTheme\\.typography\\."
    - from: ".github/workflows/build.yml"
      to: "app/src/main/java/com/example/ui/screens"
      via: "grep fontSize outside Type.kt fails build"
      pattern: "Enforce typography gate"
---

<objective>
Lock typography to 5 sizes / 3 weights max, zero inline copy(fontSize=) outside Type.kt per D-02 Typography Semantic Scale Only — achieve Pillar 4 Typography 4/4.
Purpose: Eliminate 22 inline fontSize overrides that break type rhythm; hero 44sp and micro 10sp become first-class tokens.
Output: Collapsed Type.kt + migrated screens + blocking CITypography gate (sequenced after Palette gate to avoid parallel build.yml race).
</objective>

<execution_context>
@C:/Users/sisid/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/sisid/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-ui-token-lockdown/02-CONTEXT.md
@.planning/phases/02-ui-token-lockdown/02-REVIEWS.md
@K:/apna-hisaab/UI-REVIEW.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Collapse Type scale to 5 sizes / 3 weights with displayHero + labelMicro</name>
  <read_first>
    - K:/apna-hisaab/app/src/main/java/com/example/ui/theme/Type.kt
  </read_first>
  <action>
    Collapse BaseTypography per D-02: keep 5 sizes / 3 weights max. Remove duplicate 32sp (displayMedium vs headlineLarge — keep one as displayMedium 32sp Black, drop duplicate headlineLarge 32sp or map headlineLarge to 28sp). Keep sizes: display 32sp, headline 22sp, title 20sp/17sp, body 17sp/15sp, label 12sp/10sp plus hero extension. Weights: only Black (numbers/display), Bold (titles/CTAs/labels), Medium (body) — demote ExtraBold/SemiBold to Bold, Normal to Medium where applicable. Unify FontFamily to SansSerif for all (remove Default for bodyLarge/bodyMedium). Preserve emojiPlatformStyle. Keep existing displayHero 44sp Black -1.5ls and labelMicro 10sp Bold as extended vals (or add inside Typography extension if preferred). Document max 5+2 extended tokens. Verify Type.kt defines ≤7 styles total in BaseTypography plus 2 extended displayHero/labelMicro, and grep FontWeight shows only Black/Bold/Medium.
  </action>
  <verify>
    <automated>grep -n "FontWeight\." app/src/main/java/com/example/ui/theme/Type.kt | sort | uniq -c; echo "---"; grep -n "fontSize =" app/src/main/java/com/example/ui/theme/Type.kt | wc -l | xargs echo "Type.kt fontSize defs:"; grep -c "displayHero\|labelMicro" app/src/main/java/com/example/ui/theme/Type.kt | xargs echo "hero/micro tokens:"</automated>
  </verify>
  <done>Type.kt defines ≤7 BaseTypography styles + displayHero/labelMicro, only FontWeight Black/Bold/Medium, single SansSerif family, no duplicate 32sp, no inline drift</done>
</task>

<task type="auto">
  <name>Task 2: Replace all inline fontSize overrides with semantic tokens</name>
  <read_first>
    - K:/apna-hisaab/app/src/main/java/com/example/ui/screens/AajScreen.kt
    - K:/apna-hisaab/app/src/main/java/com/example/ui/screens/KahaniScreen.kt
    - K:/apna-hisaab/app/src/main/java/com/example/ui/screens/SapnaScreen.kt
    - K:/apna-hisaab/app/src/main/java/com/example/ui/screens/HisaabScreen.kt
    - K:/apna-hisaab/app/src/main/java/com/example/ui/screens/SettingsScreen.kt
    - K:/apna-hisaab/app/src/main/java/com/example/ui/theme/Type.kt
  </read_first>
  <action>
    Delete all 22 inline `copy(fontSize=)` / `fontSize =` overrides in screens per D-02 specifics:
    - AajScreen: 626 fontSize 44sp -> MaterialTheme.typography.displayLarge or `displayHero`, 516 24sp -> headlineMedium, 585 14sp -> titleMedium, 615 11sp -> labelSmall, 1296 9sp -> labelMicro 10sp, plus any other 44/11/9sp hits. Use MaterialTheme.typography.* only, never copy(fontSize=).
    - KahaniScreen: 21sp -> titleLarge, plus 6 hits total migrating to titleLarge/bodyMedium/labelSmall.
    - SapnaScreen: 18sp -> titleLarge, plus 4 hits.
    - SettingsScreen/HisaabScreen: remaining inline sizes to titleMedium/bodyLarge/labelSmall as appropriate.
    Verify visual unchanged: hero still 44sp via displayHero token, badge 11sp via labelSmall, micro 9-10sp via labelMicro. Do NOT redesign type hierarchy, only tokenize values.
  </action>
  <verify>
    <automated>grep -R "fontSize =" --include="*.kt" app/src/main/java/com/example/ui/screens 2>/dev/null | wc -l | xargs echo "inline fontSize hits in screens (must be 0):"; grep -R "copy(fontSize" --include="*.kt" app/src 2>/dev/null | wc -l | xargs echo "copy(fontSize hits (must be 0):"; grep -R "MaterialTheme.typography\." --include="*.kt" app/src/main/java/com/example/ui/screens | wc -l | xargs echo "token usages:"</automated>
  </verify>
  <done>grep -R "fontSize =" in app/src/main/java/com/example/ui/screens returns 0; grep -R "copy(fontSize" in app/src returns 0; screens use MaterialTheme.typography.displayHero/labelMicro/labelSmall/titleMedium etc exclusively</done>
</task>

<task type="auto">
  <name>Task 3: Add blocking CI typography gate (sequenced append after Palette gate)</name>
  <read_first>
    - K:/apna-hisaab/.github/workflows/build.yml
  </read_first>
  <action>
    Append blocking CI step "Enforce typography gate" immediately after "Enforce Palette token gate" and before Build Release APK. The step must FAIL (exit 1) if any screen contains inline fontSize override — run: `if grep -R "fontSize =" --include="*.kt" app/src/main/java/com/example/ui/screens | grep -q .; then echo "::error::Inline fontSize outside Type.kt"; grep -R "fontSize =" --include="*.kt" app/src; exit 1; fi; if grep -R "copy(fontSize" --include="*.kt" app/src | grep -q .; then echo "::error::copy(fontSize) outside Type.kt"; exit 1; fi; echo "Typography gate passed"`. Do NOT use warn-only — this is blocking per reviews consensus to achieve immediate 4/4. Since this plan is wave 2 depends_on 02-01, the build.yml edit is serialized after 02-01's Palette gate, eliminating parallel race. Verify ordering: Palette gate -> Typography gate -> Spacing gate (next plan) -> Build Release APK.
  </action>
  <verify>
    <automated>grep -n "Enforce Palette\|Enforce typography\|Enforce Spacing\|Build Release APK" .github/workflows/build.yml; echo "---gate body---"; grep -A6 "Enforce typography gate" .github/workflows/build.yml | head -20</automated>
  </verify>
  <done>build.yml contains blocking typography gate after Palette gate; CI fails if any fontSize/copy(fontSize) in screens; ordering Palette -> Typography -> (Spacing next) -> Build verified</done>
</task>

</tasks>

<verification>
- `grep -R "fontSize" app/src --include="*.kt"` → only app/src/main/java/com/example/ui/theme/Type.kt contains fontSize definitions
- `./gradlew :app:assembleRelease` succeeds, visual hierarchy identical but tokenized (44sp hero via displayHero, 10sp micro via labelMicro)
- UI-REVIEW re-audit P4 Typography checklist: ≤5 sizes / 3 weights, zero inline overrides, detekt-style gate enforced → P4 scores 4/4
</verification>

<success_criteria>
Pillar 4 Typography re-audit 4/4: Type.kt collapsed to ≤7 styles / 3 weights with displayHero 44sp + labelMicro 10sp, zero inline fontSize in screens, blocking CI gate enforces scale; wave 2 sequenced after 02-01 eliminates build.yml parallel race.
</success_criteria>

<output>
Create `.planning/phases/02-ui-token-lockdown/02-02-SUMMARY.md` when done
</output>
