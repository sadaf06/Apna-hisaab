---
wave: 2
depends_on: ["02-01"]
files_modified:
  - app/src/main/java/com/example/ui/theme/Type.kt
  - app/src/main/java/com/example/ui/screens/AajScreen.kt
  - app/src/main/java/com/example/ui/screens/KahaniScreen.kt
  - app/src/main/java/com/example/ui/screens/SapnaScreen.kt
  - app/src/main/java/com/example/ui/screens/SettingsScreen.kt
  - app/src/main/java/com/example/ui/screens/HisaabScreen.kt
autonomous: true
requirements: [typography-lockdown]
---

# Plan 02-02: Typography — Lock Scale, Remove Inline Overrides

**Objective:** 5 sizes / 3 weights max, zero `copy(fontSize=)` outside `Type.kt`.

**Gap:** UI-REVIEW P4 Typography 2/4 — 7 sizes, 6 weights, 22 inline `fontSize =` overrides.

## Tasks

### Task 1: Collapse Type scale
<read_first>
- K:/apna-hisaab/app/src/main/java/com/example/ui/theme/Type.kt
</read_first>
<action>
- In BaseTypography, keep 5 sizes: display 32sp Black, headline 22sp ExtraBold→Bold, title 20sp/17sp Bold, body 17sp Medium / 15sp Normal, label 12sp Bold / 10sp Bold. Remove duplicate 32sp (headlineLarge vs displayMedium) keep one. Remove ExtraBold/SemiBold keep Black/Bold/Medium. Add `displayHero = TextStyle(44sp, Black, -1.5ls)` and `labelMicro = 10sp Bold` if needed for hero/badge (max 5+2). Unify FontFamily to SansSerif for all (remove Default). Keep EmojiSupportMatch.
</action>
<acceptance_criteria>
- Type.kt defines ≤7 styles total, ≤3 distinct FontWeight values, no duplicate 32sp definitions, contains displayHero and labelMicro if used
- `grep "FontWeight\." app/src/main/java/com/example/ui/theme/Type.kt` shows only Black, Bold, Medium
</acceptance_criteria>

### Task 2: Replace inline fontSize overrides with semantic tokens
<read_first>
- K:/apna-hisaab/app/src/main/java/com/example/ui/screens/AajScreen.kt
- K:/apna-hisaab/app/src/main/java/com/example/ui/screens/KahaniScreen.kt
- K:/apna-hisaab/app/src/main/java/com/example/ui/screens/SapnaScreen.kt
- K:/apna-hisaab/app/src/main/java/com/example/ui/screens/HisaabScreen.kt
- K:/apna-hisaab/app/src/main/java/com/example/ui/theme/Type.kt
</read_first>
<action>
- Replace `AajScreen:624 fontSize=44.sp` with `MaterialTheme.typography.displayLarge` or `Typography.displayHero`, `516 24.sp` with `headlineMedium`, `585 14.sp` with `titleMedium`, `615 11.sp` with `labelSmall`, `1296 9.sp` with `labelMicro`. Similarly fix Kahani 21sp→titleLarge, Sapna 18sp→titleLarge, Settings/Hisaab inline sizes. Delete all `copy(fontSize=` in screens (22 hits). Use `MaterialTheme.typography.*` only.
</action>
<acceptance_criteria>
- `grep -R "fontSize =" --include="*.kt" app/src/main/java/com/example/ui/screens` returns 0 hits
- `grep -R "copy(fontSize" --include="*.kt" app/src` returns 0 hits
- Visual: Aaj hero still 44sp, badges 11sp via tokens, not inline
</acceptance_criteria>

### Task 3: Add detekt/ktlint guard
<read_first>
- K:/apna-hisaab/.github/workflows/build.yml
</read_first>
<action>
Add CI step before build:
- name: Enforce typography gate
  run: |
    if grep -R "fontSize =" --include="*.kt" app/src/main/java/com/example/ui/screens | grep -q .; then echo "::error::Inline fontSize outside Type.kt"; grep -R "fontSize =" --include="*.kt" app/src; exit 1; fi
    if grep -R "copy(fontSize" --include="*.kt" app/src | grep -q .; then echo "::error::copy(fontSize) outside Type.kt"; exit 1; fi
    echo "Typography gate passed"
</action>
<acceptance_criteria>
- build.yml contains typography gate step
- CI fails if screen contains fontSize override, passes when only Type.kt defines sizes
</acceptance_criteria>

## Verification
- `grep -R "fontSize" app/src` → only Type.kt
- `./gradlew :app:assembleRelease` succeeds, UI hierarchy unchanged but tokenized
- UI-REVIEW re-audit P4 Typography → 4/4

## must_haves
- ≤5 sizes / 3 weights in Type.kt
- Zero inline fontSize in screens
- CI gate enforces scale
