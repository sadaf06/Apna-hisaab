---
wave: 2
depends_on: ["02-01"]
files_modified:
  - app/src/main/java/com/example/ui/theme/Dimens.kt
  - app/src/main/java/com/example/MainActivity.kt
  - app/src/main/java/com/example/ui/screens/AajScreen.kt
  - app/src/main/java/com/example/ui/screens/HisaabScreen.kt
  - app/src/main/java/com/example/ui/screens/SapnaScreen.kt
  - app/src/main/java/com/example/ui/screens/SettingsScreen.kt
  - app/src/main/java/com/example/ui/screens/KahaniScreen.kt
  - app/src/main/java/com/example/ui/components/GlassUi.kt
  - .github/workflows/build.yml
autonomous: true
requirements: [spacing-lockdown]
---

# Plan 02-03: Spacing — Enforce Dimens Scale, Unify Clearance & Radii

**Objective:** All paddings/gaps/radii via Dimens, single bottom clearance, 3 radii systems.

**Gap:** UI-REVIEW P5 Spacing 2/4 — 40% raw dp (14/18/10/6/140/180dp), 3 bottom clearances, 4 radius systems.

## Tasks

### Task 1: Extend Dimens with clearance + micro
<read_first>
- K:/apna-hisaab/app/src/main/java/com/example/ui/theme/Dimens.kt
</read_first>
<action>
Add to Dimens.kt:
- `val micro = 2.dp` (adopt 2 if needed, else keep documented as grid exception)
- `val navBarClearance = 96.dp` (64dp bar + 18dp inset + 14dp breathing, replaces scattered 140/180dp)
- Ensure `screenPadding=20dp (xl)`, `sectionGap=16dp (lg)` documented, keep `xs 4, sm 8, md 12, lg 16, xl 20, xxl 24, xxxl 32`
- Shapes already sm 12, md 16, lg 20, xl 28, pill 50 — keep, document primary xl 28, secondary md 16, pill 50 only.
</action>
<acceptance_criteria>
- Dimens.kt contains `micro`, `navBarClearance`, all existing tokens unchanged, Shapes uses only radiusSm/Md/Lg/Xl/pill
</acceptance_criteria>

### Task 2: Replace raw dp with Dimens tokens
<read_first>
- K:/apna-hisaab/app/src/main/java/com/example/MainActivity.kt
- K:/apna-hisaab/app/src/main/java/com/example/ui/screens/AajScreen.kt
- K:/apna-hisaab/app/src/main/java/com/example/ui/screens/HisaabScreen.kt
- K:/apna-hisaab/app/src/main/java/com/example/ui/screens/SapnaScreen.kt
</read_first>
<action>
- MainActivity: `TopBar padding 20.dp→Dimens.screenPadding, 14.dp→Dimens.md (12) or lg (16)`, `LiquidGlassNavBar padding 20/18/8.dp → screenPadding/xl/xl`, `glassShadow 18/8dp → xl/sm`, `NavGlassItem 10/6dp → lg/sm`, `itemWidth padding 4/6dp → xs/sm`.
- AajScreen: `18dp border → xl`, `10dp vertical → md`, `6dp spacing → sm`, `140dp Spacer / LazyColumn bottom 140dp → Dimens.navBarClearance + WindowInsets.navigationBars`, `18dp quick-add pill padding → lg`.
- HisaabScreen: `contentPadding bottom 140dp → navBarClearance`, `DiaryEntryCard gaps 2/8dp → micro/sm`.
- SapnaScreen: `FAB bottom 100dp + 24dp end → navBarClearance + 16dp + WindowInsets`, `EmptyGoalCard height 140dp → navBarClearance`, `bottom 180dp → navBarClearance`.
- Settings/Kahani: similar 140dp → navBarClearance, `1.5dp/1.8dp borders → border (1.2dp) or hairline (1dp)`.
- GlassUi: `BlurLockable blur 14dp → Dimens.xl (16) or keep 14 but via Dimens.micro*7` if adopted.
</action>
<acceptance_criteria>
- `grep -E "[0-9]+\.dp" app/src/main/java/com/example/MainActivity.kt | grep -v "Dimens"` → 0 hits (all via Dimens)
- `grep -E "140\.dp|180\.dp" app/src --include="*.kt"` → 0 hits (all via navBarClearance)
- Bottom clearance single value 96dp + WindowInsets, not 140/180
</acceptance_criteria>

### Task 3: Unify radii + add CI gate
<read_first>
- K:/apna-hisaab/app/src/main/java/com/example/ui/theme/Dimens.kt
- K:/apna-hisaab/.github/workflows/build.yml
</read_first>
<action>
- Audit radius: primary cards `Shapes.xl (28dp)` (LiquidGlassCard, HisaabLiquidGlassCard), secondary/inset `Shapes.md (16dp)` (GlassCard), pills `Shapes.pill (50dp)` / `Shapes.sm (12dp)`. Replace `RoundedCornerShape(16.dp)` ad-hoc with `Shapes.md`, `RoundedCornerShape(28.dp)` with `Shapes.xl`, `CircleShape` for FAB/Avatar keep.
- Add CI gate before build:
  - name: Enforce Dimens gate
    run: |
      if grep -R "140\.dp\|180\.dp" --include="*.kt" app/src | grep -q .; then echo "::error::Raw 140/180dp found, use Dimens.navBarClearance"; exit 1; fi
      # Optional: warn on raw 14.dp/18.dp outside Dimens (allow micro 2)
      echo "Spacing gate passed"
</action>
<acceptance_criteria>
- build.yml contains spacing gate step
- All primary cards use Shapes.xl, secondary md, pills pill/sm, zero 140/180dp literals
- CI fails if raw bottom clearance present
</acceptance_criteria>

## Verification
- `grep -R "140\.dp\|180\.dp" app/src` → 0 hits
- `./gradlew :app:assembleRelease` succeeds, layout identical but tokenized
- UI-REVIEW re-audit P5 Spacing → 4/4

## must_haves
- Dimens.navBarClearance single source
- Zero 140/180dp literals
- CI gate enforces spacing discipline
